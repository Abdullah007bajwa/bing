import 'package:flutter/material.dart';

/// Centralized Vexa palette to keep the UI consistent across the app.
class VexaColors {
  static const background = Color(0xFF0D0D0D); // near black
  static const surface = Color(0xFF1A1A1A); // surface cards
  static const accent = Color(0xFFE8A320); // amber/gold
  static const danger = Color(0xFFCC3300); // red-orange

  static const activeGreen = Color(0xFF4CAF50);
  static const emergencyGradientStart = Color(0xFF1A0000);

  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF888888);
  static const textHint = Color(0xFF555555);
  static const divider = Color(0xFF1F1F1F);

  static const bubbleSent = Color(0xFFE8A320);
  static const bubbleReceived = Color(0xFF1E1E1E);

  static const receipt = Color(0xFF666666);

  static const inputBorder = Color(0xFF333333);
}

