<?xml version="1.0" encoding="utf-8"?>
<!--
  Android Network Security Config
  - Only allow TLS 1.3 connections
  - Pin to our Render relay domain (update SHA-256 hash after first deploy)
  - Block all cleartext / HTTP traffic
-->
<network-security-config>

    <!-- Block all cleartext (HTTP) app-wide -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Certificate pinning for the relay server -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">ghost-relay.onrender.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
        <!--
            Certificate pinning — update sha256 after issuing real cert on Render:
            Run: openssl s_client -connect ghost-relay.onrender.com:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | base64
        -->
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">REPLACE_WITH_REAL_CERT_PIN_AFTER_DEPLOY==</pin>
        </pin-set>
    </domain-config>

    <!-- Supabase -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">supabase.co</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

</network-security-config>
